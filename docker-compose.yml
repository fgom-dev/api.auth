version: '3.1'

services:
  postgres:
    image: postgres
    container_name: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=semog
      - POSTGRES_PASSWORD=Dr4inY0u
      - POSTGRES_DB=mydb
    volumes:
      - pgdata:/data

volumes:
  pgdata:
    driver: local
